#!/bin/zsh
# vim: set filetype=zsh syntax=zsh

for zshrc_fragment in "${ZDOTDIR:-$HOME}"/.zshrc.d/*.zsh(N); do
  # shellcheck disable=SC1090
  source "$zshrc_fragment"
done
unset zshrc_fragment

if [ -f ~/.zshrc.local ]; then
  # shellcheck disable=SC1090
  . ~/.zshrc.local
fi
