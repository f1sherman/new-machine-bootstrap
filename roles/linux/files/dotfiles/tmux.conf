setw -g mode-keys vi

set -sg escape-time 0

# Enable extended keys (CSI u protocol) for modified key combos (Ctrl+Enter, etc.)
set -g extended-keys on
set -g extended-keys-format csi-u

# Use F12 as prefix to avoid conflict with local tmux (nested sessions)
set -g prefix F12
unbind C-b
bind F12 send-prefix

# Use zsh as a login shell. Unlike macOS (where /etc/zprofile basically just
# runs path_helper), Linux distros commonly put meaningful things in
# /etc/profile and /etc/profile.d/*.sh (corporate CAs, vendor SDK paths,
# nvm, etc.), so we keep the login path here and accept the small startup
# cost in exchange for not silently breaking system-level env setup.
set -g default-shell /usr/bin/zsh
set -g default-command "/usr/bin/zsh -l"

# Capture PATH from attaching shell and run restore maintenance
set-hook -g client-attached 'run-shell -b "$HOME/.local/bin/tmux-client-attached \"$PATH\""'

# Automatic session naming based on context (git branch, directory, SSH).
# NOTE: pane-title-changed is intentionally NOT hooked — CLIs like Claude Code
# and Codex update pane titles multiple times per second (spinner frames),
# which firehoses this hook and saturates the tmux server. Focus-in and
# session-changed cover every case where the user actually interacts.
set-hook -g pane-focus-in 'run-shell -b "$HOME/.local/bin/tmux-session-name #{pane_id}"'
set-hook -g client-session-changed 'run-shell -b "$HOME/.local/bin/tmux-session-name #{pane_id}"'

bind -n M-\\ split-window -h -c '#{pane_current_path}'
bind -n M-- split-window -v -c '#{pane_current_path}'
bind -n M-= select-layout even-horizontal
bind -n M-+ select-layout even-vertical
bind -n M-t new-window -c '#{pane_current_path}'
bind -n M-n next-window
bind -n M-p previous-window

bind-key -nr    M-Up resize-pane -U
bind-key -nr  M-Down resize-pane -D
bind-key -nr  M-Left resize-pane -L
bind-key -nr M-Right resize-pane -R

is_ssh="ps -o args= -t '#{pane_tty}' | grep -qE '(^ssh |gh (codespace|cs) ssh|devpod ssh)'"

bind-key -n M-PPage copy-mode -u

bind-key -n M-y if-shell "$is_ssh" 'send-keys M-y' 'copy-mode'

bind-key -n M-w display-popup -E -w 95% -h 90% tmux-switch-window
bind-key -n M-8 display-popup -E -w 60% -h 60% tmux-switch-session
bind-key -n M-9 switch-client -p
bind-key -n M-0 switch-client -n

set-option -g history-limit 100000


# Global status bar: off. Each pane instead gets its own status line on the
# bottom border, styled like the old global status bar (black bg, yellow
# branch, cyan path, white host-tag).
set -g status off
set -g status-interval 5
set -g pane-border-status bottom

# Border line colors: dark grey for inactive, bright cyan for active.
# Both use black bg so the pane-border-format content sits on a consistent
# dark bar, matching the old status-bar's status-bg/status-fg.
set -g pane-border-style 'bg=black,fg=colour240'
set -g pane-active-border-style 'bg=black,fg=colour51'

set -g pane-border-format '#[bg=black,fg=yellow] [#{pane_index}]#(~/.local/bin/tmux-pane-branch #{pane_current_path})#[fg=cyan] #{b:pane_current_path} #[fg=white]#(~/.local/bin/tmux-host-tag) '

setw -g monitor-activity on
set -g visual-activity on

set -g set-titles on
set -g set-titles-string '#S: #W'
setw -g automatic-rename off
set -g allow-rename off
set -g window-status-current-format ' | #($HOME/.local/bin/tmux-window-name #{pane_tty})'

# Required by neovim
set-option -g focus-events on

# copy/paste
set -g set-clipboard on
bind-key -T copy-mode-vi 'v' send -X begin-selection
bind-key -T copy-mode-vi 'y' send -X copy-pipe-and-cancel "osc52-copy"
bind-key -T copy-mode-vi 'Y' send -X copy-pipe-and-cancel "osc52-copy-oneline"
unbind -T copy-mode-vi Enter
bind-key -T copy-mode-vi Enter send -X copy-pipe-and-cancel "osc52-copy"
bind-key -T copy-mode 'y' send -X copy-pipe-and-cancel "osc52-copy"

# Smart pane switching with awareness of Vim splits.
# See: https://github.com/christoomey/vim-tmux-navigator
#
# Uses tmux's format-based if-shell (-F) against #{pane_current_command}
# instead of shelling out to `ps | grep`. Each C-h/j/k/l keystroke then
# costs zero forks, which matters a lot when fork+exec is slow on the host.
is_vim='#{m:*vim*,#{pane_current_command}}'
bind-key -n 'C-h' if-shell -F "$is_vim" 'send-keys C-h'  'select-pane -L'
bind-key -n 'C-j' if-shell -F "$is_vim" 'send-keys C-j'  'select-pane -D'
bind-key -n 'C-k' if-shell -F "$is_vim" 'send-keys C-k'  'select-pane -U'
bind-key -n 'C-l' if-shell -F "$is_vim" 'send-keys C-l'  'select-pane -R'
bind-key -n 'C-\' if-shell -F "$is_vim" 'send-keys C-\\'  'select-pane -l'

bind-key -T copy-mode-vi 'C-h' select-pane -L
bind-key -T copy-mode-vi 'C-j' select-pane -D
bind-key -T copy-mode-vi 'C-k' select-pane -U
bind-key -T copy-mode-vi 'C-l' select-pane -R
bind-key -T copy-mode-vi 'C-\' select-pane -l

# swap a pane with the current pane
bind-key s display-panes\; command-prompt -p "pane #: " "swap-pane -t '%%'"

# Pick files
unbind-key C-p
unbind-key -T root C-p

bind-key -T root C-p \
  run-shell '/bin/echo #{pane_id} > /tmp/tmux-pick-files-pane' \; \
  if-shell -F "$is_vim" \
    'send-keys C-p' \
    'display-popup -E -w 95% -h 95% -d "#{pane_current_path}" pick-files'

# Enable true color support
set -g default-terminal "tmux-256color"
set -ga terminal-overrides ",xterm-256color:Tc"
set -ga terminal-overrides ",*256col*:Tc"
set -ga terminal-overrides ",*:Ss=\\E[%p1%d q:Se=\\E[2 q"
set -g update-environment "DISPLAY SSH_ASKPASS SSH_AGENT_PID SSH_CONNECTION WINDOWID XAUTHORITY COLORTERM"
setenv -g COLORTERM truecolor

# Enable OSC 8 hyperlinks (requires tmux 3.4+)
set -ga terminal-features "*:hyperlinks"

# Tmux plugin manager and plugins
# Set plugin path (required for tpm scripts to work without tmux running)
set-environment -g TMUX_PLUGIN_MANAGER_PATH "~/.tmux/plugins/"
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'

# tmux-resurrect settings
set -g @resurrect-strategy-nvim 'session'
set -g @resurrect-capture-pane-contents 'on'
set -g @resurrect-processes '"claude --continue" ssh "~nvim" "~vim"'

# tmux-continuum settings
set -g @continuum-restore 'on'
set -g @continuum-save-interval '5'

# Initialize tmux plugin manager (keep this line at the very bottom)
run '~/.tmux/plugins/tpm/tpm'
