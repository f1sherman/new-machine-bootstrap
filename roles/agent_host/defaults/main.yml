---

agent_host_install_user_home: true
agent_host_user_home: "{{ ansible_facts['user_dir'] | default(ansible_facts['env']['HOME']) }}"
agent_host_install_pr_creation_skills: true
agent_host_raw_pr_creation_block_reason: "Use the _pull-request skill to create pull requests; do not call raw PR creation commands directly."
agent_host_runtime_homes: []
