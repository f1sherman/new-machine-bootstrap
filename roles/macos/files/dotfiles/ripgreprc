# Case-insensitive, unless upper-case letters are used
--smart-case

# Don't clog-up terminal with super long lines
--max-columns=100

# Sort results by path (disables parallelism, might be slower)
--sort path

# Try to guess which regex engine to use
--engine auto
