# ChatGPT Chrome Tab Cleanup

This unpacked extension is deployed to `~/.local/share/chrome-tab-gc-extension`.

To load it in Chrome:

1. Open `chrome://extensions`.
2. Turn on Developer mode.
3. Click **Load unpacked**.
4. Select `~/.local/share/chrome-tab-gc-extension`.
