# Pi upgrade implementation plan

Status: self-approved. Execute directly; this is a small configuration change.

**Goal:** Install Pi 0.86.1 by explicit request and shorten future update eligibility.
**Architecture:** Existing pinned mise/Aube install plus scoped Renovate rule.
**Tech stack:** Ansible YAML, Renovate JSON, GitHub Actions.
**Spec:** `docs/superpowers/specs/2026-09-20-pi-upgrade-24h-design.md`

## Constraints

Keep other packages at seven days. Keep Aube paranoid mode, exact pins, existing
review/merge gates, running sessions, and HNP replay pins. No live model calls.

## Task 1: Version and update eligibility

Files: `vars/tool_versions.yml`, `renovate.json`, `.github/workflows/renovate.yml`.
Consumes npm publication timestamps; produces pinned Pi and companion versions.

- [x] Set `tool_versions.runtimes.pi_coding_agent: 0.86.1`.
- [x] Add a package rule matching custom.regex and
  @earendil-works/pi-coding-agent with `minimumReleaseAge: "1 day"`.
- [x] Change existing workflow cron from `23 3 * * *` to `23 * * * *`.
- [x] Run Renovate validator, YAML parse, and focused configuration rendering.
  No retained test for declarative strings; provisioning is the relevant boundary.

## Task 2: Verify installation and publish

- [x] Inspect normal provisioning options and source-worktree selection.
- [x] Run focused managed-tool provisioning if safely available; otherwise use
  the closest normal provisioning boundary and record the exact blocker.
- [x] Require CLI version 0.86.1 and pi-server/pi-client imports at that version.
  Permit only the explicit one-time Pi release-package age exception. Preserve
  unrelated dependency checks and all trust checks.
- [x] Review diff, commit, create GitHub PR through z-pull-request, and arm monitor.
  Report actual deployment and release eligibility separately from PR status.

## Verification record

Renovate config validator passed (existing fileMatch migration warning; native RE2
unavailable, RegExp fallback). YAML/JSON parsed. Full bin/provision with the
existing become-password file succeeded on dev: 247 ok, 20 changed, zero failed.
First bounded start-at attempt skipped required facts; first full attempt lacked
sudo input. Both failed before changes and were retained. The successful normal
run is /tmp/provision-20260920-182100.log.

mise exec -- pi --version reports 0.86.0. Pi server/client Unix imports passed.
Offline model catalog lists openai-codex/gpt-6-astra; no auth/model call performed.
The active parent remains 0.85.1 until restarted; do not conflate process version
with the newly installed executable. No laptop or service host was provisioned.

Brian then explicitly requested 0.86.1. The ordinary install correctly failed
the 24-hour age gate. A temporary exception covered only pi-coding-agent, pi-ai,
pi-tui, pi-agent-core, pi-server, pi-client, pi-protocol, pi-telemetry and chord
under @earendil-works. An initial exception omitted pi-telemetry and failed closed.
The complete exception run succeeded: 247 ok, 17 changed, zero failed, recorded
in /tmp/provision-20260920-184718.log. The task source was restored byte-for-byte;
no age exception is committed. CLI and all three package manifests report
0.86.1, and server/client Unix imports pass. Existing live model evidence remains
unchanged. GitHub PR586 contains this pin follow-up.
